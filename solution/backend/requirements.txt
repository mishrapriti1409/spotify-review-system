# Core API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.9.0
python-multipart>=0.0.12

# LLM — direct Groq SDK (no LangChain needed)
groq>=0.11.0

# Embeddings — ONNX-based (no PyTorch; ~80 MB RAM vs ~500 MB for torch+sentence-transformers)
# chromadb[onnx] bundles ONNXMiniLM_L6_V2 (all-MiniLM-L6-v2 via onnxruntime)
onnxruntime>=1.18.0
tokenizers>=0.19.0

# Vector Store
chromadb>=0.5.18

# Ingestion
# App Store: scraped directly via requests (iTunes RSS API) — no library needed
google-play-scraper>=1.2.7
praw>=7.8.1
requests>=2.28.0
beautifulsoup4>=4.12.0

# Analytics & NLP
vaderSentiment>=3.3.2
scikit-learn>=1.6.0

# Utilities
python-dotenv>=1.0.1
tqdm>=4.66.0
